# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-nodejs

name: Node.js CI Ecommerce Shop dev

on:
  push:
    branches: [ "master" ]

jobs:
  build:

    runs-on: self-hosted

    strategy:
      matrix:
        node-version: [22.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
    - uses: actions/checkout@v4
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
    - run: npm ci
    - name: run pm2 restart server
      env:
        BACKEND_HOST: ${{ vars.BACKEND_HOST }}
        FRONTEND_HOST: ${{ vars.FRONTEND_HOST}}
        PORT: ${{ vars.PORT }}
        MONGO_URI: ${{ vars.MONGO_URI }}
        REDIS_URL: ${{ vars.REDIS_URL}}
        GOOGLE_CLIENT_ID: ${{ vars.GOOGLE_CLIENT_ID }}
        GOOGLE_CLIENT_SECRET: ${{ vars.GOOGLE_CLIENT_SECRET }}
        SESSION_SECRET: ${{ vars.SESSION_SECRET }}
        AWS_BUCKET_NAME: ${{ vars.AWS_BUCKET_NAME}}
        AWS_BUCKET_REGION: ${{ vars.AWS_BUCKET_REGION}}
        AWS_BUCKET_ACCESS_KEY: ${{ vars.AWS_BUCKET_ACCESS_KEY }}
        AWS_BUCKET_SECRET_KEY: ${{ vars.AWS_BUCKET_SECRET_KEY}}
      run: pm2 restart ecommerce-shop-dev --update-env
